--- a/src/Mod/Anthracite/Gui/AgentBridge.cpp
+++ b/src/Mod/Anthracite/Gui/AgentBridge.cpp
@@ -103,6 +103,24 @@ AgentBridge::AgentBridge(SidebarControll
     });
 }
 
+AgentBridge::~AgentBridge()
+{
+    draftTimer.stop();
+    if (runtime.state() == QProcess::NotRunning) {
+        return;
+    }
+
+    send(QStringLiteral("session.shutdown"));
+    runtime.closeWriteChannel();
+    if (!runtime.waitForFinished(1500)) {
+        runtime.terminate();
+        if (!runtime.waitForFinished(1000)) {
+            runtime.kill();
+            runtime.waitForFinished(1000);
+        }
+    }
+}
+
 void AgentBridge::submit(const QString& prompt)
 {
     queuedPrompt = prompt;
--- a/src/Mod/Anthracite/Gui/AgentBridge.h
+++ b/src/Mod/Anthracite/Gui/AgentBridge.h
@@ -30,6 +30,7 @@ class AgentBridge: public QObject
 
 public:
     explicit AgentBridge(SidebarController* controller, QObject* parent = nullptr);
+    ~AgentBridge() override;
 
 private Q_SLOTS:
     void submit(const QString& prompt);
--- a/src/Mod/Anthracite/Gui/Sidebar.cpp
+++ b/src/Mod/Anthracite/Gui/Sidebar.cpp
@@ -612,11 +612,11 @@ Sidebar::Sidebar(QWidget* parent)
     : QWidget(parent)
     , sidebarController(this)
     , agentBridge(new AgentBridge(&sidebarController, this))
+    , quickView(new QQuickWidget(this))
 {
     setWindowTitle(tr("Anthracite"));
     setMinimumWidth(320);
 
-    auto* quickView = new QQuickWidget(this);
     quickView->setResizeMode(QQuickWidget::SizeRootObjectToView);
     quickView->rootContext()->setContextProperty(
         QStringLiteral("anthraciteController"),
@@ -629,6 +629,14 @@ Sidebar::Sidebar(QWidget* parent)
     layout->addWidget(quickView);
 }
 
+Sidebar::~Sidebar()
+{
+    delete quickView;
+    quickView = nullptr;
+    delete agentBridge;
+    agentBridge = nullptr;
+}
+
 SidebarController* Sidebar::controller()
 {
     return &sidebarController;
--- a/src/Mod/Anthracite/Gui/Sidebar.h
+++ b/src/Mod/Anthracite/Gui/Sidebar.h
@@ -11,6 +11,7 @@
 
 #include "ConversationModel.h"
 
+class QQuickWidget;
 
 namespace AnthraciteGui
 {
@@ -167,11 +168,13 @@ class Sidebar: public QWidget
 
 public:
     explicit Sidebar(QWidget* parent = nullptr);
+    ~Sidebar() override;
     SidebarController* controller();
 
 private:
     SidebarController sidebarController;
     AgentBridge* agentBridge;
+    QQuickWidget* quickView;
 };
 
 Sidebar* ensureSidebar();
